"""Phase 1 scaffolding for the KaryoAI deep-learning upgrade (Mask R-CNN track).

This package is additive. It does not modify, import from in a breaking way,
or replace `karyo_ai`, which remains the working classical baseline used by
`karyo_ai_cli.py` and `streamlit_app.py`. See docs/MIGRATION_FROM_BASELINE.md
for how the two relate and when the baseline gets formally renamed
`ClassicalBaseline` for side-by-side evaluation (a later phase, once
evaluate_segmentation.py exists to compare them fairly).
"""
